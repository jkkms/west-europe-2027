# 디자인 개선 의뢰서 — 서유럽 2027 여행 앱

디자인 작업을 맡길 때 이 문서를 통째로 넘기면 됩니다.
기술적 배경은 [CLAUDE.md](CLAUDE.md)에, 실물은 https://jkkms.github.io/west-europe-2027/ 에 있습니다.

---

## 1. 이 앱이 무엇인가

2027년 2월 3~17일 서유럽 13박 15일 가족여행 일정 앱. 단일 HTML 파일.

**쓰는 사람이 누구인지가 이 앱의 모든 결정을 좌우합니다.**

- **주 사용자는 40~50대 부모님**입니다. 만든 사람이 아니라 부모님이 보시는 화면입니다.
- 대부분 **휴대폰 세로**로 봅니다. 데스크톱은 계획 세울 때만.
- **현지에서 데이터 없이** 열어보는 경우가 있습니다(오프라인 지원됨).
- 한국어. 날짜·시각·거리 같은 수치는 고정폭 글꼴로 통일돼 있습니다.

그래서 **작고 촘촘한 것보다 크고 성긴 것**이 낫습니다. 이미 한 번 "사진이 너무 작다",
"토글 버튼이 너무 작다"는 지적을 받고 키운 이력이 있습니다.

## 2. 화면 구성

하단 탭 4개(900px 이상에서 상단 탭으로 전환).

| 탭 | 하는 일 |
|---|---|
| 일정 | 2/3~2/17 15줄. 누르면 그날 계획·주의사항·여유 옵션·관광지 사진이 펼쳐짐. 도시 필터 칩. "이 날 동선 지도에서 보기" 버튼 |
| 지도 | Leaflet 지도에 핀 47개(공항·기차역·관광지·숙소). 도시 필터. 동선 선과 구간별 대중교통 링크 |
| 항공 | 왕복 2편 정보와 좌석 배치도(접힘) |
| 준비 | 예약 체크리스트 표 17항목 — 예약 시작일·항목·예매처 3열. 진행 막대. 인쇄 버튼 |

## 3. 손대면 안 되는 것

디자인 개선이라도 아래는 유지해야 합니다. 전부 **실제로 문제가 생겨서** 정해진 규칙입니다.

**구조**
- **단일 HTML 파일.** 빌드 도구·프레임워크·npm 없음. CSS와 JS가 파일 안에 들어 있습니다.
  이 구조를 깨면 GitHub Pages 배포와 오프라인 지원이 같이 무너집니다.
- 외부 의존은 Google Fonts, Leaflet(cdnjs), OSM 타일, 위키미디어 API 뿐. **더 늘리지 마세요.**
- **앱 안에 사진 업로드 UI를 넣지 마세요.** 사진은 `photos/` 폴더에 파일로만 넣습니다.

**절대 넣으면 안 되는 CSS**
- **전역 `svg` 선택자 금지.** 예전에 `figure svg{width:100%}` 같은 규칙이 Leaflet 내부
  오버레이까지 잡아 지도 경로선이 사라졌습니다. SVG는 `.caret svg` 처럼 클래스로만 지정.
- **여러 화면이 공유하는 클래스는 컨테이너 id로 좁힐 것.** `.chip` 을 전역으로 잡았다가
  일정 탭과 지도 탭이 서로의 선택 표시를 지웠습니다. `#chips .chip` 처럼 쓰세요.
- 다크 모드 지도 반전은 `#map.nightmap .leaflet-tile-pane` 에만. `#map` 전체에 걸면
  핀과 팝업까지 뒤집힙니다.

**접근성 — 이미 맞춰 놓았으니 되돌리지 마세요**
- 펼침 행·좌석 배치도·체크박스는 전부 **진짜 `<button>`** 입니다. `div`에 click만 달면
  키보드로 못 씁니다. `aria-expanded`/`aria-checked` 를 함께 갱신합니다.
- 탭은 `role="tablist"`, 좌우 화살표·Home·End 동작합니다.
- 사진창은 `role="dialog" aria-modal`, Tab 이 창 안에 갇히고, 닫으면 원래 자리로 초점이 돌아옵니다.
- `:focus-visible` 윤곽선, `prefers-reduced-motion` 대응.
- `viewport` 에 `maximum-scale=1` 넣지 마세요. 확대가 막힙니다.

**반응형 — 이미 잡혀 있는 경계**
- ~374px(아이폰 SE): 예약 표 날짜 열을 104px 로 줄이고 요일을 아랫줄로. **이 폭에서
  예매처 도메인이 잘리는지 반드시 확인하세요.** 한 번 15개가 잘린 적 있습니다.
- ~699px: 관광지 카드 1열, 사진창 2열
- 700~899px: 카드 2열, 사진창 3열
- 900px~: 상단 탭 전환, 카드 3열, 사진창은 중앙 모달
- 1280px~: 본문 1080px

**인쇄**
- `@media print` 로 종이 일정표가 나옵니다(A4 약 6장). 지도 탭은 빼고 일정·항공·준비만.
- 준비 표의 체크칸은 **남겨야 합니다.** 종이 체크리스트에 손으로 표시할 네모가 없으면 쓸모없습니다.
- Chromium 은 flex 안에서 `break-inside:avoid` 를 무시합니다. 날짜 행은 `display:table` 로 둘 것.

## 4. 지금의 디자인 언어

바꿔도 되지만, 바꾼다면 **일관되게** 바꿔 주세요.

```
--ink #16202b   --slate #5a6b7a   --paper #eef1f4   --card #fff
--frost #d5dee5 --soft #e6ebef    --alpine #2e6e8e  --deep #1d4f68
--ember #b64f2b --moss #4a7a5e    --plum #7c5cbf    --mossbg #e9f0eb
```

- 겨울 알프스의 차가운 청회색. **크림색·테라코타 조합은 피할 것**(원래 의도와 어긋납니다).
- 본문 IBM Plex Sans KR 300 / 제목 Gowun Batang 700 / **날짜·시각·수치는 전부 IBM Plex Mono**
- `prefers-color-scheme: dark` 대응 — 두 모드 모두 확인해야 합니다.
- 의미 색: 주의사항 ember(주황) · 여유 옵션 moss(초록) · 강조 alpine(청) · 숙소 plum(보라)
- 핀 색: 공항 ember / 기차역 moss / 관광지 alpine / 숙소 plum

## 5. 개선을 바라는 지점

정답을 정해 두지 않았습니다. 아래는 관찰된 약점입니다.

1. **일정 탭이 길다.** 15줄 아코디언이라 펼치면 스크롤이 깁니다. 하루를 훑는 더 나은
   방법이 있을지.
2. **정보 위계가 평평하다.** 계획 문장·주의사항·여유 옵션·사진 카드가 비슷한 무게로
   쌓여 있습니다. 무엇을 먼저 봐야 하는지 덜 분명합니다.
3. **여행 중 화면이 없다.** 지금은 "오늘"을 강조하고 그 줄로 스크롤할 뿐입니다.
   현지에서 열었을 때 오늘 할 일만 크게 보여주는 화면이 있으면 좋겠습니다.
4. **지도 탭의 정보가 많아졌다.** 칩 + 동선 막대 + 구간 목록 + 지도 + 범례 + 안내문이
   세로로 쌓입니다. 정리가 필요합니다.
5. **관광지 카드가 밋밋하다.** 사진 + 이름 + 한 줄. 표시 전용이라 클릭 동작은 넣지 않습니다.
6. 빈 상태 처리가 거칩니다(사진 없음 등).

## 6. 고칠 때 확인하는 법

```bash
node tools/sw-test.js       # 오프라인 동작 (브라우저 없이)
```

그 밖에 바꾼 뒤 눈으로 확인할 것:
- 320px / 375px / 768px / 1280px 네 폭
- 밝은 모드 / 어두운 모드
- 네 탭 모두, 가로 스크롤이 생기지 않을 것
- 인쇄 미리보기(Ctrl+P)
- 지도: 도시 칩 전환, 동선 보기, 핀 팝업

## 7. 이 앱의 전제

**여유로운 페이스가 최우선입니다.** 부모님과 가는 여행이라 하루에 확정 일정을 적게 잡고,
시간이 남을 때만 가는 곳은 "여유 옵션"으로 시각적으로 분리해 두었습니다.
**옵션을 확정 일정처럼 보이게 만들지 마세요** — 하루가 빡빡해 보이면 이 여행의 전제가 무너집니다.

일정·좌표·예약일 같은 **내용은 디자인 작업에서 바꾸지 마세요.** 근거를 달아 검증한 값들이고,
근거는 CLAUDE.md 에 적혀 있습니다.
