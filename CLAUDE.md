# 서유럽 가족여행 2027 — 프로젝트 인수인계

부모님(40~50대)과 함께하는 서유럽 13박 15일 여행 앱. 여유로운 페이스가 최우선.

## 파일 구조

```
index.html            단일 파일 앱. HTML·CSS·JS 전부 내장
sw.js                 서비스워커 (오프라인)
manifest.webmanifest  홈 화면 추가용
icons/                앱 아이콘 192·512. tools/mkicon.py 로 다시 생성
photos/               관광지 사진. {id}-{1~6}.jpg|jpeg|png|webp
seats/                항공 좌석 배치도 (tw407-outbound.jpg, tw406-return.jpg)
app.html              옛 주소. index.html 로 넘겨주기만 함
```

`https://jkkms.github.io/west-europe-2027/` 에 GitHub Pages(main / root)로 배포.
외부 의존은 Google Fonts, Leaflet(cdnjs), 지도 타일(OSM→Esri 폴백), 위키미디어 공용 API.

**`sw.js`의 `VERSION`은 캐시에 이미 들어간 파일을 바꿨을 때만 올린다.** 좌석 배치도·아이콘·
매니페스트가 여기 해당한다(같은 출처 파일은 캐시 우선이라 안 올리면 옛 파일이 계속 나온다).
`index.html`만 고쳤다면 올릴 필요 없다 — 화면 이동은 네트워크 우선이라 새로고침하면 갱신된다.
검증함: VERSION 그대로 두고 `index.html`을 바꾸면 새로고침에 반영되지만, 같은 조건에서
`seats/*.jpg`를 바꾸면 옛 이미지가 계속 나왔다. 버전을 올리면 앱 하단에 "새 버전이
준비됐습니다"가 뜨고, 사용자가 누를 때 새로고침된다.

## 확정 사실 — 임의로 바꾸지 말 것

### 항공 (티웨이, 오픈조, 결제 완료)

| 구간 | 편명 | 출발 | 도착 | 소요 | 좌석 |
|---|---|---|---|---|---|
| ICN→BCN | TW0407 | 2/3 수 11:30 인천 T1 | 2/3 수 18:25 엘프라트 T2 | 14h55m | 이코노미 3-4-3 · 39H·39K·40J·40K |
| FCO→ICN | TW0406 | 2/16 화 19:00 피우미치노 T3 | 2/17 수 14:50 인천 T1 | 11h50m | 프리미엄 이코노미 2-4-2 · 7G·7H·8G·8H |

- 두 좌석 모두 **맨 앞열(벌크헤드)**. 발밑 수납 없음, 팔걸이 고정, 앞에 유아 요람 거치 위치.
- 기종 B777-300ER. 프리미엄 이코노미 36~38인치/19.5인치, 이코노미 32인치/17.2인치.
- **기내 엔터테인먼트 콘텐츠 없음.** 태블릿 준비 필요.

### 일정 (13박, 도시 간 이동 전 구간 기차)

| 도시 | 숙박 | 기간 |
|---|---|---|
| 바르셀로나 | 3박 | 2/3–2/6 |
| 파리 | 3박 | 2/6–2/9 |
| 인터라켄 | 3박 | 2/9–2/12 |
| 피렌체 | 2박 | 2/12–2/14 |
| 로마 | 2박 | 2/14–2/16 |

2027년 2월 1일은 월요일. 날짜·요일 계산 시 확인할 것.

## 설계 결정과 근거

**휴관일이 일정 순서를 결정한다.** 아래 배치는 유일해가 아니면 제약이 큼. 순서를 바꾸려면 반드시 재검증.

- 피카소 미술관 = 월요일 휴관 → 2/5(금)
- 오르세 = 월요일 휴관 → 2/7(일)에 배치
- 오랑주리 = 화요일 휴관 → 2/7(일) 여유 옵션으로 성립
- 루브르 = 화요일 휴관 → 2/8(월)에 배치
- 우피치·아카데미아 = 월요일 휴관, 두오모 = 일요일 관광입장 불가 → 2/13(토)
- 보케리아 시장 = 일요일 휴무 → 2/5(금) 여유 옵션으로 성립
- 바티칸 박물관 = 일요일 휴관 → 2/15(월)
- 산탄젤로 성 = 월요일 휴관 → 2/15 불가, 2/16(화) 여유 옵션

2/14는 일요일이라 피렌체 중앙시장 영업 여부가 불확실하다. 앱에는 "확인하세요"로만
적어 뒀다. 확정되면 문구를 고칠 것.

**인터라켄 3박인 이유.** 융프라우요흐가 날씨로 실패할 수 있는 유일한 일정. 2/10과 2/11 중 예보 좋은 날 선택. 흐리면 그린델발트 피르스트로 전환. 하더쿨름·쉬니게플라테는 겨울 운휴.

**로마 2박인데 충분한 이유.** 귀국편이 19:00 출발이라 2/16이 온전한 하루로 살아남. 단 공항 16:00 도착 기준 시내 15:00 출발.

**몬세라트를 뺀 이유.** 왕복 이동 약 3시간, 1인 28.8~50유로. 산악 풍경은 인터라켄에서 이미 충족되고, 모데르니스메 건축은 바르셀로나에서만 볼 수 있음. 그 시간을 팔라우 데 라 무지카·산 파우 병원에 배정.

**바르셀로나→파리를 기차로 한 이유.** 직행 TGV 09:26 산츠역 출발 → 16:12 파리 리옹역 도착, 6시간 46분(두 도시 모두 CET라 시차 없음). 항공(문전 약 5시간)과 실질 차이가 작고 시내–시내 직결에 수하물 제한 없음. 편수가 적어 조기 예약 필요.

**베르사유를 뺀 이유.** 2월엔 분수가 멈추고 정원이 앙상해 실내만 보게 되는데 왕복 두 시간이 든다. 어느 날의 여유 옵션으로도 넣기 어려워 완전히 제외했다(2026-08-01 결정). `LOC`·`SINFO`에서도 지웠으니 되살리려면 좌표부터 다시 넣어야 한다.

**바르셀로나를 북쪽/도심으로 나눈 이유(2026-08-28 재배치).** 원래는 사그라다(북)+카사바트요(도심) / 팔라우(도심)+산파우(북)+구엘(북) 로 묶여 있어 이틀 다 도심과 북부를 왕복했다. 좌표로 재보니 **사그라다↔산파우 1.3km, 카사바트요↔팔라우 1.0km** 로 붙어 있는데 서로 다른 날로 갈라져 있던 것. 북쪽(사그라다·산파우·구엘)과 도심(팔라우·피카소·카사바트요)으로 다시 묶어 **6.5km → 5.8km 로 줄이면서 관광지는 5곳에서 7곳으로 늘렸다.** 사그라다에서 산 파우까지는 가우디 거리 보행로로 이어진다. **이 두 덩어리를 다시 섞지 말 것.**

덤으로 두 가지가 맞아떨어진다. 보케리아(일요일 휴무)가 금요일인 2/5 에 남고, **몬주익 야경이 산츠역 방향**이라 2/6 아침 09:26 기차 전날 동선으로 맞는다. 반대로 구엘 공원은 도시 북쪽 끝이라 출발 전날 밤에 두면 안 된다.

**파리 2/8 은 노트르담부터 시작한다.** 루브르를 먼저 보면 동쪽 시테섬으로 갔다가 다시 서쪽 개선문으로 되돌아와 9.7km 가 되고, 노트르담을 아침에 먼저 보면 8.5km 다.

**로마 2/14 저녁은 트레비 → 판테온 → 나보나 순서다.** 나보나를 먼저 가면 판테온을 지나쳤다 되돌아온다(0.91km vs 1.18km).

**동선 보기(`.droute`).** 일정 카드의 "이 날 동선 지도에서 보기"를 누르면 지도로 넘어가
그날 `spots` 순서대로 선을 긋고 정류장에 번호를 찍는다. 선은 흰 테두리 위에 점선을 얹어야
지도 위에서 보인다. **`alt:true` 인 날은 버튼을 만들지 않는다** — 2/11 예비일처럼 "셋 중
하나"를 고르는 날에 순서선을 그으면 셋을 다 가는 것처럼 보인다. 새 날을 추가할 때
대안 관계면 `alt:true` 를 꼭 붙일 것.

`showDayRoute` 는 `showTab('map')` 전에 `mapFitted=true` 를 세운다. 안 그러면 탭 전환이
부르는 자동 맞춤이 뒤늦게 끼어들어 방금 그린 선을 지운다(`fit` 이 `clearRoute` 를 부른다).

**여유 옵션(`opt`)을 따로 둔 이유.** 부모님과 가는 여행이라 확정 일정을 적게 잡고, 시간이 남을 때만 가는 곳을 시각적으로 분리했다. 확정 일정은 사진 카드로, 옵션은 초록 글상자의 목록으로 나온다. **옵션을 확정 일정으로 올리지 말 것** — 하루를 빡빡하게 만들면 이 여행의 전제가 무너진다.

## 숙소(`STAYS`)

다섯 곳 모두 정해졌다(2026-08-30). 좌표 출처가 두 갈래다.

- **에어비앤비 네 곳** — 페이지의 schema.org JSON-LD 에 실린 `latitude`/`longitude`.
  **체크인 전까지는 대략 위치다**(에어비앤비가 일부러 흐려 놓는다). `approx:true` 로 표시하고
  팝업과 안내문에 "대략 위치"라고 적어 뒀다. 정확한 문 앞 주소로 바꾸지 말 것 —
  예약 확인 메일에만 있고, 그건 공개 저장소에 넣을 것이 아니다.
- **피렌체 호텔** — 실제 주소(Lungarno del Tempio 44)를 Nominatim 으로 지오코딩한 값이라 정확하다.

에어비앤비 페이지는 `WebFetch` 로는 403 이고 브라우저로는 열린다. 좌표는
`"latitude":<수>,"longitude":<수>` 패턴으로 찾으면 된다.

링크에서 **추적용 꼬리표(`source_impression_id`, `linkhub_token`, 공유 시점의
`checkin`/`checkout`)는 떼고** 넣었다. 다시 받을 때도 뗄 것.

## 지도 화면 맞춤에서 공항을 뺀다

`fit()` 은 `t!=='air'` 인 핀으로만 경계를 잡는다. **로마 피우미치노는 시내에서 25km** 라
이것까지 담으려다 줌이 10 까지 멀어졌고, 그 배율에서는 시내 핀 10개가 서로 겹쳐 누를 수가
없었다(겹치는 쌍 32개). 공항을 빼니 줌 12, 겹치는 쌍 5개가 됐다. 바르셀로나도 같은 이유로
11 → 12. **공항 핀은 지도에 그대로 있고 프레이밍에만 안 쓴다.**

남은 겹침은 300m 이내로 실제 붙어 있는 곳들(판테온↔나보나 등)이라 확대하면 풀린다.

## 인쇄

`@media print` 로 종이 일정표를 만든다. 지도 탭은 통째로 빼고 일정·항공·준비를 한 장에
이어 붙인다(A4 약 6장). 준비 탭 체크칸은 **남긴다** — 종이 체크리스트인데 손으로 표시할
네모가 없으면 쓸모가 없다. 예매처 링크와 좌석 배치도 이미지는 뺀다.

과거 인쇄가 깨졌던 두 원인을 피한다. 전역 `svg` 선택자를 쓰지 않고, Chromium 이 flex 안에서
`break-inside:avoid` 를 무시하므로 날짜 행을 `display:table` 로 둔다.

## 지도 타일 — CARTO 를 쓰지 말 것

**CARTO basemaps 는 API 키를 요구하도록 바뀌었다(2026-08 확인).** 키 없이 부르면 타일마다
"API KEY REQUIRED" 워터마크가 찍혀 나온다. 되돌리지 말 것.

**이 실패는 폴백이 못 잡는다.** 워터마크 타일은 4xx 가 아니라 **200 으로 정상 응답**하므로
`tileerror` 가 아예 발생하지 않는다. "실패하면 갈아탄다"는 폴백은 이렇게 조용히 망가지는
경우를 못 막는다. 공급자를 바꿀 때는 폴백을 믿지 말고 눈으로 타일을 볼 것.

지금은 **OSM 표준**이 기본이고, 6회 실패하면 Esri 회색 지도(+ 라벨 레이어)로 넘어간다.
Esri 를 기본으로 삼지 않은 이유는 **라벨이 거의 없기 때문**이다 — z14 파리에서 구 이름만
나오고 도로명·랜드마크가 없어 길을 찾을 수 없다. 앱 색감(청회색)에는 Esri 가 맞지만
부모님이 길을 볼 지도라 가독성을 택했다. Stadia 는 키가 필요해(401) 탈락.

어두운 테마는 **`#map.nightmap .leaflet-tile-pane` 에만 filter 를 걸어** 타일을 반전시킨다.
`#map` 전체에 걸면 핀과 팝업까지 반전되니 반드시 tile-pane 으로 좁힐 것.

## 주의 — 과거에 실제로 터진 버그

- **클래스 이름 충돌.** 섹션에 `.transit` 클래스를 붙였는데 이동일 행이 이미 `day transit`을 쓰고 있어, 인쇄 시 모든 카드가 이동일마다 잘렸음. 선택자는 `section.transit`처럼 좁힐 것.
- **전역 `svg` 선택자 금지.** `figure.map svg{width:100%}` 같은 규칙이 Leaflet 내부 오버레이 SVG까지 잡아 경로선이 사라졌음. 인쇄용 도식에는 `.map-print`처럼 전용 클래스를 쓸 것.
- **Chromium은 flex 컨테이너 안에서 `break-inside:avoid`를 무시.** 인쇄용으로는 `display:table` 계열로 전환.
- **선택자 충돌 재발.** 일정 탭과 지도 탭이 둘 다 `.chip`을 쓰는데 지도 쪽 핸들러가
  `document.querySelectorAll('.chip')`으로 전체를 잡아, 한쪽 도시를 고르면 다른 쪽 선택
  표시가 지워졌다. 화면이 여러 개인 앱에서 공통 클래스를 다룰 때는 `#chips .chip`처럼
  **컨테이너 id로 반드시 좁힐 것.**
- **숨은 패널에서 만든 지도는 `fitBounds` 가 망가진다.** 지도 탭이 `display:none` 인 채로
  Leaflet 지도를 만들면 컨테이너 크기가 0 이라 `fitBounds` 가 말도 안 되는 줌을 계산하고
  `maxZoom` 에 걸려 굳는다. 실제로 "전체"를 골랐는데 이탈리아 시골이 z14 로 확대돼 있었다.
  `invalidateSize()` 만으로는 안 고쳐진다 — 크기가 잡힌 뒤 **fit 을 다시 호출**해야 한다.
  단 매번 다시 맞추면 사용자가 옮겨 둔 화면이 튀므로 `mapFitted` 로 첫 번째만 한다.
  **고정 시간으로 기다리지 말 것.** 처음엔 `setTimeout(...,120)` 으로 뒀는데, 탭이 보이게
  된 뒤에도 폭이 0 으로 잡히는 경우가 있었다. 지금은 `fitWhenSized()` 가 `ResizeObserver`
  로 크기가 실제로 잡히는 순간을 기다리고, 관찰자가 놓칠 때를 위해 100ms 재시도(최대 2초)를
  함께 돌린다.
- **사진창의 늦은 응답이 남의 창을 덮어썼다.** 사진을 불러오는 동안 창을 닫고 다른 곳을
  열면, 먼저 요청한 결과가 나중에 도착해 새 창의 내용을 갈아치웠다. `lbToken` 을 두고
  기다린 뒤 토큰이 바뀌었으면 버린다. 비동기 결과를 화면에 쓰기 전에는 항상 확인할 것.
- **`aria-modal` 만으로는 키보드가 안 갇힌다.** 사진창이 떠 있어도 Tab 이 뒤 화면으로
  새어 나갔다. Tab/Shift+Tab 을 직접 처리해 창 안에서 돌게 했다.
- **사진을 못 찾으면 "불러오는 중"이 영원히 남았다.** `fillThumb` 이 성공했을 때만 자리표시자를
  바꾸고 실패 경로를 안 만들어 뒀다. 오프라인이거나 위키미디어가 막히면 그대로 굳는다.
  비동기 자리표시자에는 **반드시 실패 문구도 만들어 둘 것.**
- **숨은 패널 안의 `loading="lazy"` 이미지는 안 뜬다.** 항공 탭 좌석 배치도가
  `display:none` 상태로 시작해 lazy 관찰자에 화면 밖으로 등록됐고, 탭을 눌러 보이게 해도
  로드가 시작되지 않았다. 탭 전환으로만 노출되는 이미지에는 lazy를 붙이지 말고
  `width`/`height`로 자리만 잡아 둘 것.

## 사진

`photos/{id}-1.jpg` 형식. id 목록은 `photos/README.txt` 참조. 사용자 사진이 있으면 우선 사용하고, 없으면 위키미디어 공용에서 자동으로 채움(`USE_COMMONS=false`로 끌 수 있음). 자동 사진에는 저작자·라이선스 표기가 붙음. **앱 안에 사진 추가 UI를 넣지 말 것.** 파일이나 코드로만 추가.

## 미결 항목

1. **바르셀로나→파리 직행 TGV 미예약.** 09:26→16:12 편. 편수가 적어 최우선.
2. 2/7은 2027년 2월 첫째 일요일 → 오르세 무료 개방일이며 **이날은 시간 예약이 의무**(확인함).
3. 설 연휴 2/6~9가 파리~인터라켄 구간과 겹침.
4. 피렌체 중앙시장의 일요일(2/14) 영업 여부 미확인.
5. **저장소가 공개다.** 숙소 위치·예약 링크가 공개 저장소에 들어 있다. 사용자가 알고 넣은
   것이지만, 신경 쓰인다면 `STAYS` 의 `url` 을 빼거나 좌표를 도시 중심으로 뭉뚱그릴 수 있다.

## 좌표 추가하는 법

`LOC` 에 새 장소를 넣을 때 좌표를 **추측하지 말 것.** 위키피디아 좌표 API 로 조회한다.

```
https://en.wikipedia.org/w/api.php?action=query&prop=coordinates&format=json&redirects=1&titles=제목1|제목2
```

영문 제목에 좌표가 없으면 현지어 위키(ca/it/fr)를 볼 것 — 보케리아·몬주익 성·중앙시장이
그랬다. 넣은 뒤에는 **붙어 있어야 할 이웃 지점과 거리를 재서 대조**한다(노트르담↔생트샤펠
0.44km, 시뇨리아↔우피치 0.22km 처럼). 자릿수가 틀리면 여기서 바로 드러난다.

2026-08-28 에 13곳(피카소·보케리아·카사밀라·몬주익성·노트르담·생트샤펠·오랑주리·개선문·
시뇨리아·중앙시장·판테온·나보나·스페인광장)을 이 방법으로 넣었다.

## 서비스워커는 브라우저 없이 검증한다

```
node tools/sw-test.js
```

`sw.js` 를 가짜 `caches`·`fetch` 위에서 실행해 **캐시 규칙·오프라인 폴백·404 미저장**을
확인한다. 크롬 없이 돌아가므로 서비스워커가 막힌 환경에서도 쓸 수 있다(실제로 브라우저
패널에서 등록이 막혀 이 하네스로 검증했다 — 한 줄짜리 최소 워커도 등록에 실패하면
앱 문제가 아니라 환경 문제다).

`sw.js` 를 고치면 이 스크립트부터 돌릴 것. 특히 **404 를 캐시하지 않는지**는 꼭 확인한다 —
캐시되면 나중에 `photos/` 에 사진을 넣어도 영영 안 보인다.

## 검증 습관

날짜를 바꿀 때는 요일과 휴관일을 코드로 재계산해 대조할 것.
표 열 너비를 만졌으면 320px 에서 도메인이 잘리는지 볼 것.
동선을 바꿀 때는 좌표로 거리를 재서 대조할 것 — 감으로 판단하지 말 것. 운영시간·요금은 추측하지 말고 공식 사이트를 확인하거나 미확인으로 명시할 것.

---

# 재구축 사양

이 문서만으로 앱을 처음부터 다시 만들 수 있도록 정리한 사양이다.

## 앱 구성

단일 HTML 파일. 하단(모바일) 또는 상단(데스크톱) 탭 4개.

| 탭 | 내용 |
|---|---|
| 일정 | 2/3~2/17 날짜 15줄. 누르면 그날 일정·주의사항·관광지 카드가 펼쳐짐. 도시별 필터 칩. 오늘 날짜는 강조 후 자동 펼침 |
| 지도 | Leaflet 실제 지도. 공항·기차역·관광지·숙소 핀. 도시별 필터. 핀 클릭 시 구글지도·길찾기·사진 |
| 항공 | 편명·시각·터미널·소요시간·좌석·배치도 이미지. 배치도 클릭 시 확대 |
| 준비 | 예약 체크리스트 **표**. 예약 시작일·항목·예매처 3열, 항목 이름을 누르면 설명 행이 열림. 체크는 항목 id 로 localStorage 저장 |

관광지 카드는 **표시 전용**. 링크도 클릭 동작도 없다. 사진과 이름, 한 줄 정보만.

## 데이터 모델

```js
DAYS  = [{d:'2/3', w:'수', c:'바르셀로나', t:'한줄요약',
          plan:'그날 설명', spots:['id',…], note:['라벨','내용'],
          opt:['여유 옵션 한 줄', …]}, …]   // opt 는 없으면 생략
LOC   = [{c:'도시', t:'air|rail|see', id:'식별자', n:'한글명',
          lat:0, lng:0, q:'커먼즈 검색어'}, …]
SINFO = {id:'휴관일·소요시간·체력 등 한 줄 정보'}
TODOS = [{id, t:'항목', open:'YYYY-MM-DD'|null, sure:true|false,
          d:'설명', site:'표시명', url:'공식 예매처'}, …]
STAYS = [{city, name, lat, lng}]     // 숙소, 현재 비어 있음
PHOTOS= {id:['경로 또는 URL']}        // 비우면 폴더 규칙 사용
```

## 좌표

초기 29곳은 구글 플레이스 조회값, 2026-08-28 에 더한 13곳은 위키피디아 좌표 API 조회값.
추가 방법은 위의 "좌표 추가하는 법" 참조.

```
bcnair          41.29834,   2.08001   바르셀로나 엘프라트 공항   air
sants           41.37896,   2.13983   바르셀로나 산츠역   rail
sagrada         41.40363,   2.17436   사그라다 파밀리아
guell           41.41449,   2.15269   구엘 공원
batllo          41.39170,   2.16492   카사 바트요
palau           41.38758,   2.17531   팔라우 데 라 무지카
santpau         41.41550,   2.17473   산 파우 병원
picasso         41.38522,   2.18089   피카소 미술관
boqueria        41.38180,   2.17155   보케리아 시장
pedrera         41.39528,   2.16167   카사 밀라
montjuic        41.36340,   2.16620   몬주익 성
lyon            48.84430,   2.37438   파리 리옹역   rail
eiffel          48.85837,   2.29448   에펠탑
louvre          48.86061,   2.33764   루브르 박물관
orsay           48.85996,   2.32656   오르세 미술관
sacre           48.88670,   2.34310   사크레쾨르 대성당
notredame       48.85306,   2.35000   노트르담 대성당
saintechapelle  48.85528,   2.34500   생트샤펠
orangerie       48.86385,   2.32254   오랑주리 미술관
arc             48.87378,   2.29503   개선문
ilkost          46.69043,   7.86905   인터라켄 오스트역   rail
jungfrau        46.54746,   7.98211   융프라우요흐
first           46.62513,   8.04178   그린델발트 피르스트
lauter          46.59351,   7.90910   라우터브루넨
smn             43.77667,  11.24792   피렌체 SMN역   rail
duomo           43.77315,  11.25596   두오모
uffizi          43.76779,  11.25531   우피치 미술관
accademia       43.77681,  11.25864   아카데미아 미술관
vecchio         43.76793,  11.25314   베키오 다리
michelangelo    43.76293,  11.26506   미켈란젤로 광장
signoria        43.76972,  11.25556   시뇨리아 광장
mercato         43.77667,  11.25333   중앙시장
termini         41.90129,  12.50158   로마 테르미니역   rail
fco             41.80346,  12.25192   피우미치노 공항   air
vatican         41.90649,  12.45364   바티칸 박물관
stpeter         41.90217,  12.45394   성 베드로 대성당
colosseo        41.89021,  12.49223   콜로세움
trevi           41.90093,  12.48331   트레비 분수
angelo          41.90306,  12.46628   산탄젤로 성
pantheon        41.89860,  12.47680   판테온
navona          41.89900,  12.47310   나보나 광장
spagna          41.90600,  12.48210   스페인 광장
```

## 디자인

겨울 알프스의 차가운 청회색 계열. 크림색·테라코타 조합은 피할 것.

```
--ink #16202b   --slate #5a6b7a  --paper #eef1f4  --card #fff
--frost #d5dee5 --soft #e6ebef   --alpine #2e6e8e --deep #1d4f68
--ember #b64f2b --moss #4a7a5e   --plum #7c5cbf
```

- 본문 IBM Plex Sans KR 300, 제목 Gowun Batang 700, **날짜·시각·수치는 전부 IBM Plex Mono**
- `prefers-color-scheme: dark` 대응
- 핀 색: 공항 ember / 기차역 moss / 관광지 alpine / 숙소 plum

## 반응형

- ~699px 모바일: 하단 탭, 본문 전체 폭, 일정 관광지 카드 1열, 사진창 2열
- 700~899px: 하단 탭, 본문 660px, 관광지 카드 2열, 사진창 3열
- 900px~: **상단 탭으로 전환**, 본문 920px, 지도 560px, 관광지 카드 3열, 사진창은 중앙 모달
- 1280px~: 본문 1080px, 지도 640px

**375px 미만(아이폰 SE·구형 안드로이드)은 따로 잡아 뒀다.** 예약 표의 날짜 열을 150px 로
두면 항목 칸이 105px 로 눌려 예매처 도메인이 16개 중 15개 잘렸다. `max-width:374px` 에서
요일을 아랫줄로 내리고 날짜 열을 104px 로 줄인다. **표 열 너비를 만질 때는 320px 에서
도메인이 잘리는지 반드시 확인할 것.**

일정 탭의 관광지 카드(`.scards`/`.sthumb`)는 3:2 비율이고 **모바일에서 1열**이다. 부모님이
보시는 화면이라 작게 넣지 말 것. 2열로 되돌리면 휴대폰에서 사진이 너무 작아진다.

좌석 배치도는 `<details class="seatbox">`로 **접힌 채 시작**하고, 펼쳐도 `max-height:48vh`로
화면을 넘지 않는다. 누르면 사진창에서 원본 크기로 볼 수 있다.

`viewport`에 `maximum-scale=1`을 넣지 말 것. 확대가 막혀 접근성 문제가 생긴다.

## 펼침 화살표는 `.caret` 하나로 통일

일정 행·준비 표 항목·항공 좌석 배치도 세 곳이 **같은 `.caret`을 쓴다.** 30px 원형에
18px 셰브론이 들어가고, **닫히면 오른쪽·펼치면 90도 돌아 아래쪽**을 본다. 열린 상태는
`--alpine` 으로 채운다. 회전은 `.drow.open .caret, .brow.open .caret, .seatbox[open] .caret`
한 줄이 담당한다. 새 펼침 UI를 만들면 `▾` 같은 글자 대신 이 클래스를 쓰고, 회전 규칙에
선택자를 추가할 것. 글자 화살표는 작아서 누를 곳으로 보이지 않는다.

## 준비 표의 폭 다루기

표는 `table-layout:fixed` 다. **auto 로 두면 `th` 의 `width` 를 권고로만 받아 지정한 폭이
안 나온다** — 열 너비를 조정했는데 안 먹으면 이걸 먼저 볼 것.

예약 시작 열은 모바일 150px, 데스크톱 200px. 375px 화면에서 이 폭을 확보하면 항목 칸이
160px 밖에 안 남는데, `tickets.duomo.firenze.it` 같은 긴 주소를 그대로 넣으면 칩이 두 줄로
깨지거나 말줄임으로 잘린다. **가짜 예매 사이트를 경고해 두었으므로 도메인은 잘리면 안 된다.**

그래서 화면에는 `shortSite()` 로 `ticket.` `tickets.` `ticketing.` `billetterie.` `entrades.`
같은 예매용 서브도메인을 떼고 핵심 도메인만 보여준다(`ticket.louvre.fr` → `louvre.fr`).
가짜를 가리는 데 필요한 등록 도메인은 그대로 남고 폭은 아낀다. **`url` 은 손대지 않는다.**
모바일에서는 `↗` 도 숨긴다(칩 테두리로 이미 링크임이 보인다). 데스크톱에서는 다시 보인다.

넓은 화면에서는 예매처 링크에 `min-width:178px` 를 줘 고정폭 열로 세운다. 링크 길이가
제각각이면 링크도 화살표도 항목마다 다른 자리에 서서 지저분해진다.

## 접근성 — 유지할 것

- 펼침 행(`.dhead`)과 좌석 배치도(`.smapbtn`)는 **실제 `<button>`**. `div`에 click만 달면
  키보드로 못 연다. 여는 쪽에서 `aria-expanded`를 같이 갱신할 것.
- 체크리스트 `.chk`는 `role="checkbox"` + `tabindex="0"` + Space/Enter 처리 + `aria-checked`.
- 하단/상단 탭은 `role="tablist"`, 각 패널은 `role="tabpanel"`. 탭 전환 시 `aria-selected` 갱신.
- 사진창은 `role="dialog" aria-modal="true"`. 열 때 제목으로 초점을 옮기고, 닫을 때 원래
  누른 요소로 되돌린다.
- 칩은 `aria-pressed`로 선택 상태를 알린다.

## 외부 의존

```
폰트   fonts.googleapis.com
지도   cdnjs Leaflet 1.9.4 (leaflet.min.css / leaflet.min.js 모두 존재 확인됨)
타일   tile.openstreetmap.org → 6회 실패 시 Esri 회색 지도로 전환
사진   commons.wikimedia.org API (origin=* 로 CORS 허용)
```

## 대중교통 소요시간은 구글 지도로 넘긴다

앱에서 직접 계산하려면 Directions API 키(결제 등록)가 필요하고, **2027년 2월 시각표는 지금
조회해도 그때와 다르다.** 그래서 계산하지 않고 넘긴다. `api=1&travelmode=transit` 규격이라
키가 필요 없고, 현지에서 실시간 시각표·지연까지 반영된 결과를 보게 된다.

동선 보기를 켜면 지도 위에 구간 목록이 뜬다. 구간마다 `gTransit(a,b)`, 하루 전체는
`gDayPath(stops)`(경유지 9곳까지). **화면의 거리는 직선거리**이므로 그렇게 명시해 두었다 —
실제 도보/대중교통 거리로 착각하게 두지 말 것.

확인함(2026-08-30): 숙소→팔라우 링크가 구글 지도에서 L3 지하철 27분으로 안내되고,
경유지를 넣은 하루 전체 경로도 정상으로 열린다.

## 구글 지도 — 왜 Leaflet을 쓰는가

지도 자체를 구글 지도로 바꾸려면 Maps JavaScript API 키가 필요하고, 키 발급에 결제 카드
등록이 따른다. 게다가 공개 저장소라 키가 노출되므로 HTTP 리퍼러 제한이 필수다. 그래서
**핀·필터가 있는 지도는 Leaflet로 두고, 구글 지도는 링크로 연결**하기로 했다(2026-08-01 결정).

공식 URL 규격(`api=1`)만 쓴다. 휴대폰에 구글 지도 앱이 있으면 앱으로 열린다.

```
gPlace(L)  maps/search/?api=1&query=<영문명>      장소 페이지(영업시간·리뷰)
gDir(L)    maps/dir/?api=1&destination=<위경도>   길찾기
gArea(...) maps/@?api=1&map_action=map&center=…   도시 전체 보기
```

`gPlace`는 `LOC`의 `q`(커먼즈 검색어로도 쓰는 영문명)를 재사용한다. `q`가 없는 숙소는
위경도로 떨어진다. 새 장소를 추가할 때 `q`를 비우지 말 것.

## 예약 — 조사 결과 (조사일 2026-08-28)

`TODOS`의 각 항목은 `open`(예약 열리는 날)·`sure`(확정/추정)·`url`(공식 예매처)를 갖는다.
**`sure:false`는 "60일 전"처럼 공개된 규칙으로 계산한 값**이므로 그 무렵 재확인해야 한다.
앱에도 "추정" 배지로 표시된다. 확인하지 않은 날짜를 `sure:true`로 올리지 말 것.

### 반드시 지킬 사실

- **SNCF 국제선은 굴러가는 창구가 아니라 배치 판매다.** 2026-09-30에 열리는 배치가
  2026-12-13 ~ 2027-03-28 구간을 덮는다. 바르셀로나→파리(2/6)와 파리→인터라켄(2/9)이
  **같은 배치**라 하루에 함께 잡아야 한다. seat61의 "최대 6개월 전"만 보고 "이미 열렸다"고
  판단했다가 틀렸던 적이 있다 — 반드시 SNCF 공식 판매 개시 안내를 볼 것.
- **오르세 2/7은 2027년 2월의 첫째 일요일이다.** 무료 개방일이며 **이날만은 시간 예약이
  의무**다(평소에는 선택). 날짜를 옮기면 이 조건도 다시 계산할 것.
- **콜로세움 공식 판매처는 `ticketing.colosseo.it`.** 2024년 4월에 바뀌었고
  coopculture는 더 이상 공식이 아니다. 검색 상위에 아직 뜨니 주의.
- **바티칸·노트르담·오르세는 공식 사이트가 직접 가짜 예매 사이트를 경고**하고 있다.
  노트르담은 입장이 100% 무료라 돈을 받는 곳은 전부 사기다.
- **융프라우는 겨울에 예약 의무가 없다.** 좌석 예약 의무는 5~10월에만 적용된다.
  "당일 아침 날씨 보고 결정"이라는 일정 설계가 이 사실 위에 서 있다.

### 링크 점검

공식 예매처 15곳은 실제 응답을 확인했다. curl이 403을 주는 곳(SNCF·루브르·오르세·
노트르담·콜로세움)은 봇 차단일 뿐 실제 브라우저에서는 정상이다. **403만 보고 링크가
죽었다고 판단하지 말 것.**

### 체크 상태 공유는 하지 않기로 함

가족끼리 체크를 공유하려면 Firebase 같은 바깥 저장소가 필요한데, 계정을 새로 만들어야 해서
**붙이지 않기로 했다(2026-08-28 결정).** 체크는 각자 기기에만 남는다. 다시 요청이 오면
SDK 없이 Firebase REST(GET/PATCH + 폴링)로 붙이는 게 가장 가볍다 — 단일 파일 구조와
오프라인 동작을 깨지 않는다.

### 아직 확정 못 한 것

이탈리아 구간(2/12 인터라켄→피렌체, 2/14 피렌체→로마)은 개시일이 공개돼 있지 않다.
통상 4개월 전후지만 12/13 유럽 시각표 변경 뒤에 풀릴 수 있어 `sure:false`로 두었다.

## 오프라인 (sw.js)

한 번 연 적이 있으면 인터넷 없이도 일정·항공·준비 탭이 그대로 열린다. 지도는 이미 본
구역의 타일만 남는다. 캐시는 네 갈래로 나눠 두었다.

| 캐시 | 대상 | 방식 |
|---|---|---|
| `shell-{VERSION}` | index.html, 아이콘, 좌석 배치도 | 설치할 때 미리 받음. 화면 이동은 네트워크 우선, 실패하면 캐시 |
| `asset-{VERSION}` | 폰트, Leaflet | 캐시 우선 |
| `photo-1` | 내 사진, 위키미디어 공용 | 캐시 우선, 120개까지 |
| `tile-1` | 지도 타일 | 캐시 우선, 400장까지 |

- **200 응답만 저장한다.** 404를 캐시하면 나중에 `photos/`에 사진을 넣어도 계속 안 보인다.
- 경로는 전부 상대경로(`./`). GitHub Pages 하위 경로(`/west-europe-2027/`)에서 그대로 동작한다.
